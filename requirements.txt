# ============================================================
#  DELL AI Education App — Python Dependencies
#  Install with:  pip install -r requirements.txt
# ============================================================

# ── GUI Framework ────────────────────────────────────────────
PyQt5>=5.15.10         # Main application framework (QApplication, QProcess, etc.)
QScintilla>=2.14.1     # Advanced Code Editor engine with syntax & line numbers

# ── Computer Vision ──────────────────────────────────────────
opencv-python>=4.8.0  # cv2  — camera capture, face detection, image processing

# ── Numerical Computing ──────────────────────────────────────
numpy>=1.24.0         # np   — array operations, image matrices, normalization

# ── Machine Learning ─────────────────────────────────────────
scikit-learn>=1.3.0   # sklearn — model training / evaluation utilities
joblib>=1.3.0         # Model serialization (used for predict_class snippet)
onnxruntime>=1.15.0   # Core AI Inference Engine (used exclusively for advanced Vision Tasks)
ultralytics>=8.0.0    # YOLOv8 — standard for student-friendly object detection
torch>=2.0.0          # PyTorch — engine for advanced neural networks
torchvision>=0.15.0   # Computer Vision datasets & models (MobileNet, ResNet)
matplotlib>=3.7.0     # Metric visualization & loss curve rendering
