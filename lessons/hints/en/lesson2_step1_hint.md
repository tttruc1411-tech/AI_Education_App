# 💡 Hint: Image Filters Lab

## Goal
Convert color image to grayscale.

## Main Hint
Use the `convert_to_gray()` function to convert the image.

## Code Structure
```python
# 1. Initialize camera
# 2. In the loop:
#    - Get frame
#    - Convert to gray: gray_frame = convert_to_gray(camera_frame=...)
#    - Display grayscale image
# 3. Close camera
```

## Required Function:
- `convert_to_gray(camera_frame=...)` - Convert to grayscale

