# ============================================================
# LESSON 3: Drawing & Shapes - Step 3
# TITLE: Multiple Shapes
# TITLE_VI: Nhiều Hình Dạng
# ============================================================

# Import camera và display modules
import camera
import display

# Bước 1: Khởi tạo camera
# __BLANK__ capture_camera = camera.Init_Camera()

print("[OK] Multiple shapes ready!")

# Bước 2: Vẽ nhiều hình
while True:
    # __BLANK__ camera_frame = camera.Get_Camera_Frame(capture_camera = capture_camera)

    # __BLANK__ camera_frame = display.Draw_Rectangle(camera_frame = camera_frame, x = 50, y = 50, width = 150, height = 100, color = "green")

    # __BLANK__ camera_frame = display.Draw_Circle(camera_frame = camera_frame, center_x = 320, center_y = 240, radius = 80, color = "red")

    # __BLANK__ camera_frame = display.Draw_Rectangle(camera_frame = camera_frame, x = 450, y = 350, width = 150, height = 100, color = "yellow")

    # __BLANK__ display.Show_Image(camera_frame = camera_frame)

# Bước 3: Dọn dẹp
# __BLANK__ camera.Close_Camera(capture_camera = capture_camera)
